<table class="table border">
  <tr>
    <th>ジャンル検索</th>
  </tr>
  <% genres.each do |genre| %>
    <tr>
      <%= form_tag items_path, :method => 'get' do %>
        <td><%= link_to genre.name, items_path('search': genre.id) %></td>
      <% end %>
    </tr>
  <% end %>
</table>