<h1>Public::CartItems#index</h1>
<p>Find me in app/views/public/cart_items/index.html.erb</p>

<% if @cart_item.blank? %>
    <h1>カートが空です</h1>
<% end %>

<table>
    <tr>
        <th>商品名</th>
        <th>単価(税込)</th>
        <th>数量</th>
        <th>小計</th>
    </tr>

    <% @cart_item.each_index(1) do |cart_item, i| %>
    <tr>
        <th><%= cart_item[:name] %></th>
    </tr>
    <% end %>
</table>